CRITICAL INSTRUCTION: You are operating in the 'Arbeidskassen' monorepo — a single consolidated Next.js app at `apps/arbeidskassen`. All modules (bookdet, organisasjon, teamarea, today, backoffice, sales-portal) are route groups under `app/[locale]/(authenticated)/`. There are NO separate apps — one dev server on port 3000, one middleware, one build. Before you answer any query, read the file `AI_INSTRUCTIONS.md` in the root directory. You MUST strictly adhere to every rule defined in that file. Acknowledge this silently and proceed.